Three.js 关键帧动画
• 手动实现关键帧插值
• 支持播放、暂停、重置
• 可调速度和进度
• 同时支持位置、旋转、缩放

核心知识点：

1. 关键帧数据
   - 时间点 + 变换数据
   - 位置、旋转、缩放
   - 多帧形成动画

2. 线性插值 (Lerp)
   - 在两个关键帧之间平滑过渡
   - t ∈ [0, 1]
   - lerp(a, b, t) = a + (b - a) * t

3. 找到前后关键帧
   - 遍历找到 t 所在的区间
   - 计算局部插值参数

4. 应用到物体
   - 插值计算当前值
   - 直接应用到 mesh 属性
